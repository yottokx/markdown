# 2ペインMarkdownエディタ 開発計画

作成日: 2026-09-07

この文書は開発開始時の設計を記録したものです。2026-09-08現在、段階1〜6（以下のフェーズ0〜5）を実装済みです。uv初期化から着手し、スクロール同期、保存・編集・検索・テーマ・貼り付け・表編集、画像の外部編集・個別/一括改名・復旧、CSS付き単体HTML・出力プレビュー・PDFを追加しました。TeX数式・Mermaidは通常プレビューと出力に対応しています。数式・図の専用エディタは実装対象にしません。現在の操作はREADME.md、検証結果はVERIFICATION.mdを参照してください。

ユーザー指定により、PyInstallerの再ビルドは最終の段階7（フェーズ6）まで行いません。段階1〜2で作成した既存EXEは旧版です。

## 1. 基本方針と前提

- Windowsを初期対象とし、PySide6の通常のデスクトップアプリとして作る。
- 左はMarkdownソース、右はQWebEngineViewのライブプレビュー。QSplitterで幅を変更できる。
- 初版は1ウィンドウ1文書、複数ウィンドウ可とする。タブは後から追加できるよう、文書状態をウィンドウから分離する。
- MarkdownはCommonMarkを基礎に、表・取り消し線・タスクリスト等の実用的な拡張を明示的に有効にする。完全なGitHub互換とは称さず、対応構文をテストで固定する。
- `prompt_editor_demo`から参考にするのはインデントの操作仕様だけ。常駐・半透明・最前面表示・AI補完・独自記法・Markdownハイライターは流用しない。
- 「プレビューとは違いCSS反映」は、通常プレビューにはアプリ標準の閲覧用CSS、HTML/PDFと出力プレビューには選択した出力用CSSを適用する意味で扱う。
- 一括画像改名の対象は「現在のMDが参照している、当該MDの隣のimgフォルダ配下の画像」とする。

## 2. 最初に行うuv初期化と技術構成

開発開始時に `C:\Python\envs\markdown` で次を実行する。プロジェクト名は仮に `md-editor` とする。

```powershell
uv init --app --package --name md-editor --python 3.13
uv add pyside6 markdown-it-py mdit-py-plugins markdownify charset-normalizer pygments nh3
uv add --dev pyinstaller pytest pytest-qt ruff
```

Python 3.13を初期候補とし、依存解決と最小配布ビルドで組合せを確定する。実際に採用したバージョンを `uv.lock` に固定する。GUI起動用エントリーポイントとPyInstaller用ランチャーを用意する。[uvの初期化仕様](https://docs.astral.sh/uv/concepts/projects/init/)

| 担当 | 採用案 |
| --- | --- |
| UI | PySide6、QMainWindow、QSplitter、QDialog |
| ソース編集 | QPlainTextEdit派生クラス、QSyntaxHighlighter |
| Markdown解析 | markdown-it-py、必要な拡張だけmdit-py-plugins |
| 表示 | QWebEngineView、ローカルのHTML/CSS/JS、QWebChannel |
| HTML貼り付け | markdownify |
| HTMLの整形・許可要素の制御 | HTMLパーサーとnh3を用いた処理 |
| 文字コード推定 | BOM・UTF-8検査・charset-normalizer、手動指定で補正 |
| コードの色分け | 左はQSyntaxHighlighter、右と出力はPygmentsを候補にする |
| 設定・監視・保存 | QSettings、QFileSystemWatcher、QSaveFile |
| 配布 | PyInstaller、初期はonedir/windowed |

markdown-it-pyには表等の拡張があるが、有効な構文は採用バージョンの設定で固定する。[拡張の公式説明](https://markdown-it-py.readthedocs.io/en/latest/plugins.html)

予定構成:

```text
src/md_editor/
  app.py                  起動引数とアプリ起動
  document.py             本文・パス・文字コード・改行・変更状態
  ui/                     メイン画面、検索バー、各ダイアログ
  editor/                 キー操作、ハイライト、右クリック判定
  markdown/               構文解析、ソース範囲、レンダリング
  preview/                WebEngine、スクロール同期、描画完了管理
  services/               ファイル、貼り付け、画像、表、出力、復旧
  resources/              CSS、JS、アイコン、フォント等
tests/
  fixtures/               Markdown・クリップボード・文字コードの検証例
packaging/                PyInstaller specとランチャー
```

本文を唯一の編集元とし、プレビュー・画像参照一覧・表位置はそこから導出する。解析結果には文書の更新番号を付け、古い非同期結果を破棄する。画像と表の置換は元のソース範囲だけに適用し、文書全体を勝手に再整形しない。

## 3. 画面と日常操作

- メニュー: ファイル、編集、挿入、表示、ツール。設定はファイルメニューの単一項目から開く。
- ステータスバー: 行・列、文字コード、改行コード、保存状態。文字コードと改行コードは変更可能。
- ソース側: 行番号、現在行表示、折り返し切替、フォントサイズ変更、構文ハイライト。
- 検索: Ctrl+F、次/前、件数、大小文字、正規表現。追加提案としてCtrl+Hの置換・すべて置換を同時に実装する。
- 検索結果のソース位置へ移動した際も右プレビューを同期する。通常のプレビュー内検索は後から追加できる。
- 新規・開く・保存・名前を付けて保存、Undo/Redo、未保存終了確認を備える。
- テーマはライト・ダーク・システムと同じ。ウィジェット、ソースの色、通常プレビューをまとめて変更する。システム追従中のOS設定変更も反映する。出力用CSSの選択は独立して保存する。[Qtの色設定API](https://doc.qt.io/qtforpython-6/PySide6/QtGui/QStyleHints.html)

## 4. インデント仕様

### 4.1 参考実装で確認できた仕様

対象コードは [editor.py](C:/Python/envs/markdown/参考/prompt_editor_demo/editor.py:108)、動作例は [test_editor.py](C:/Python/envs/markdown/参考/prompt_editor_demo/tests/test_editor.py:124) にある。

| 操作 | 参考実装の挙動 |
| --- | --- |
| インデント単位 | 半角スペース2個。対象構文は行頭のスペース＋`- ` |
| 選択中Tab | 対象行がすべてリストなら、各行に2スペース追加 |
| 選択なしTab | 空のリスト項目の行末のみインデント。本文付きの行ではAI補完に接続されている |
| Shift+Tab | リストの現在行/選択行から先頭スペースを最大2個削除 |
| Enter | 行末で同じ階層の`- `を継続。最上位の空項目はリスト終了、ネストした空項目は継続 |
| 継続行Enter | 直前のリストに対応するインデントの行から、元の階層のリストを再開 |
| 空項目Backspace | まずマーカーを同じ幅の空白に替えて本文開始位置を維持。その後、条件に応じて1段浅いリストにする |
| 行選択 | 行の一部の選択も行単位で処理。選択終端が次行の先頭なら、その次行は含めない |
| Undo | 複数行のインデント変更は1回で戻す |

特に以下の操作感を引き継ぐ。`·`は説明用の半角スペース、`|`はカーソルを表す。

```text
- 親項目           - 親項目           - 親項目
··- |    → BS →   ····|    → BS →   - |
```

参考実装には番号付きリスト・引用・コードフェンスの文脈判定がなく、その制約まで引き継ぐとMarkdownエディタとして誤動作する。そのため、次の拡張を本アプリの仕様として提案する。

### 4.2 本アプリで追加する仕様案

- AI補完への分岐をなくし、本文付きのリストでもTabでインデントできるようにする。
- 通常行・コードブロックのTab/Shift+Tabは2スペース単位の字下げ/字下げ解除。選択時は行単位に処理する。Tabによるフォーカス移動は行わない。
- `*`、`+`、番号付きリスト、タスクリスト、引用との組合せをMarkdown解析結果に基づいて扱う。
- 番号付きリストの子階層は親の本文開始列に合わせる。`10. `等で2スペースでは成立しない場合は、Markdownとして有効な幅を優先する。
- Enterで箇条書き記号を維持し、番号付きリストは次番号、タスクリストは未チェック状態を挿入する。
- フェンス/インデントコードブロック内の`- `等をリストとして処理しない。コード内のEnterは行頭インデントのみ継続する。
- 参考実装の行末Enterと2段階Backspaceを基礎にし、行途中のEnter・選択文字列の置換は通常のテキスト編集として扱う。
- 日本語IME変換確定のEnterをリスト操作と誤認しない。
- ソース位置はPythonの文字位置とQtのUTF-16単位を明示的に変換する。絵文字を含む行でもカーソル・選択・置換範囲をずらさない。

## 5. コンテンツ基準の双方向スクロール

### 5.1 対応付け

1. Markdownの解析で見出し・段落・リスト項目・表・コードブロックのソース行範囲を得る。
2. プレビューの対応要素にソース行の属性を付ける。表の各行とコードの各行にも可能な範囲でアンカーを付ける。
3. 左の表示上端を「ソース行＋行内の表示位置」に変換し、対応する右の要素へ移動する。
4. 右を操作した場合は表示上端の要素からソース位置を逆算する。
5. 操作元、更新番号、同期処理中フラグで反復通知を防ぎ、ドラッグ・ホイール・キー操作のいずれも扱う。

左側では `firstVisibleBlock()` 等の座標API、通信にはQWebChannelを使う。[QPlainTextEdit](https://doc.qt.io/qtforpython-6/PySide6/QtWidgets/QPlainTextEdit.html)、[QWebChannel](https://doc.qt.io/qt-6/qtwebchannel-javascript.html)

`token.map`だけではインライン構文の正確な文字範囲は得られない。スクロール用の行対応と、画像改名・右クリック用の文字範囲は別に保持し、後者は解析規則を補って取得する。[Tokenの定義](https://markdown-it-py.readthedocs.io/en/latest/_modules/markdown_it/token.html)

折り返しが左右で異なる段落、空行、参照定義、画像の途中などは、前後のアンカーとブロック内の進捗で補間する。仕様は「同じ内容を追えること」とし、表示構造の異なる左右の全ピクセル一致は保証しない。

画像読込・フォント確定・ペイン幅/ズーム変更後に位置を再測定する。編集中のプレビュー更新では現在のアンカーを復元する。初期の更新待ちは150～250ms程度を候補とし、入力を止めないことを優先して実測調整する。

### 5.2 最終行まで上げられるスクロール

- 左は `setCenterOnScroll(True)` を最初に検証する。末尾より下へのスクロールを許す一方、カーソル追従時に中央へ寄せる動作もあるため、通常入力との相性を試作で確かめる。[Qtの仕様](https://doc.qt.io/qt-6/qplaintextedit.html#centerOnScroll-prop)
- 右は表示用の末尾余白とEOFアンカーを設け、最後の内容を表示上端まで上げられるようにする。
- 最後の行が空行や参照定義のような非表示構文でもEOF位置に同期する。
- 左の末尾スクロール、右の余白は表示だけの処理とし、MDに空行を追加しない。HTML/PDF出力にも余白を混入させない。

## 6. 貼り付け

### 6.1 通常貼り付けの判定

QMimeDataは複数形式を同時に持てるため、`text/html`の有無だけで決めない。[Qtのクリップボードデータ形式](https://doc.qt.io/qtforpython-6/PySide6/QtCore/QMimeData.html)

判定順は次を基本とする。

1. 自アプリ由来のMarkdown形式がある場合はそれを使用。文書間のコピーでは元の基準フォルダ情報も使い、管理画像の参照を貼り付け先の画像管理処理で解決する。
2. 画像ファイルのコピー、または画像単独のコピーと判定できる場合は画像貼り付け。
3. 貼り付け先がコードブロック、plainがMarkdown/コードと強く判断できる、HTMLがコード表示用の装飾である場合は `text/plain` を無加工で使用。
4. HTMLに見出し・リンク・リスト等の意味ある構造があり、Markdown/コードの根拠がなければmarkdownifyで変換。
5. 判断が曖昧なら `text/plain` を優先。plainがなければHTML変換を使用。

強い根拠としてフェンス、Markdown表、複数行の構文構造、コードの構造、HTMLの`pre/code`とplainの対応を使う。アンダースコアや括弧が含まれるだけではコードと断定しない。HTMLと画像が同居する文章のコピーを、画像だけの貼り付けにしない。

自動判定を完全にはできないため、「プレーンテキストとして貼り付け」「HTMLをMarkdownとして貼り付け」をメニューにも用意する。判定結果は短いステータス表示にとどめ、通常操作で毎回ダイアログを出さない。

markdownifyはATX見出しなど出力方針を固定する。コードコピー問題への対策としてエスケープ設定を全体で無効化せず、変換前の形式選択で対処する。コードの空白・タブ・改行を保持する。[Markdownify公式](https://github.com/matthewwithanm/python-markdownify)

### 6.2 CSV/TSVの表貼り付け

- クリップボードから表データを読み取れるとき、右クリックメニューの「表として貼り付け」を有効にする。通常のCtrl+Vでは勝手に表へ変換しない。
- ダイアログに編集可能な表を表示し、「1行目をヘッダーとして使う」「新しいヘッダー行を追加」を選べるようにする。
- 区切り文字の自動推定とCSV/TSVの手動切替、行列追加・削除、列の左/中央/右寄せに対応。
- Pythonの `csv.reader` を使い、引用符・セル内改行・末尾の空セルを処理する。先頭ゼロや日付を自動変換せず文字列として扱う。
- ヘッダーの最終判断はユーザーの選択とする。標準ライブラリの推定にも誤判定がある。[Python csvの仕様](https://docs.python.org/3/library/csv.html)
- `|`、バックスラッシュ、コード表記はGFM表として適切に処理し、セル内改行は`<br>`に変換する。変換後のプレビューを確認できるようにする。
- キャンセルで本文を変えず、確定で表全体を1回のUndo単位として挿入する。

## 7. 画像の保存・編集・改名

### 7.1 貼り付けと保存

- フォルダ名はMDの隣の `img` に固定。
- クリップボード画像はPNGで保存し、例として `img/議事録_00001.png` を挿入する。
- 画像ファイルのコピーは元形式を基本とし、`img/議事録_00002.jpg` 等で保存する。写真の不要なPNG化やGIFアニメーションの消失を避ける。
- 連番は拡張子を除いたMD名を接頭辞に、00001から既存ファイルと衝突しない番号を採番。5桁を超えたら桁を拡張する。同時操作でも上書きしない。
- パスはMarkdown用にエスケープ/エンコードし、日本語・空白・括弧を含むファイル名に対応する。
- MD未保存時は文書ID別の一時フォルダ内に `img` を作成し、暫定名で保存。プレビューはその一時フォルダを基準に表示する。
- 初回の名前を付けて保存では、保存先とMD名に基づいて画像を採番し、画像のコピーと本文参照の解決を行ってからMDを保存する。キャンセル・失敗では一時データを維持する。
- 既存MDを別フォルダへ保存する場合は、その文書で参照している管理画像だけコピーする。元のMDと画像は保持する。
- 初回保存・Save Asによる名前確定や基準フォルダ変更も履歴管理の対象にする。通常の本文Undoと保存時の参照解決を区別し、過去の履歴が指す画像・相対リンクを移行後の基準でも正しく解決する。履歴の復元に必要な画像は復旧領域にも保持し、保存とUndoの整合を確認するまで一時領域を削除しない。
- 同じフォルダでMDだけ別名保存した場合、既存画像名は維持し、新規画像から新MD名を使う。既存画像をそろえる場合は一括改名を利用する。
- `img`外の相対リンクはSave As後に意図せず別ファイルを指さないよう再計算し、移動先で維持できない参照は保存前の一覧に表示する。

### 7.2 画像構文の右クリック

カーソルの下にある画像構文を解析してメニューを決める。インライン画像と参照形式画像に対応し、コード内のサンプル表記は対象外にする。対応するHTMLの`img`要素も別の参照種別として管理する。

| 対象 | 画像を編集 | ファイル名を変更 |
| --- | --- | --- |
| 解決先がimg配下の実在するラスター画像 | 有効 | 有効 |
| img配下のSVG等の画像 | 無効 | 有効 |
| img外、外部URL、存在しない画像 | 無効 | 無効 |

相対パスを正規化し、リンク等を解決した実パスが `img` 配下に収まることを確認する。文字列が`img`で始まるだけでは対象にしない。未保存文書の一時 `img` も同じ規則で扱う。

「画像を編集」は設定された外部エディタを起動する。Windowsの初期値はペイント。引数は配列で渡し、空白入りパスを扱う。画像ファイルの更新を監視し、エディタが置換保存する場合も追跡し直してプレビューのキャッシュを更新する。外部編集自体は本文のUndoの対象に含めない。

### 7.3 個別・一括改名

- 個別改名では、同じ実画像を指す現在文書内のすべての参照を更新する。参照形式の定義を共有する通常リンクがある場合も整合を保つ。
- 一括改名はツールメニューから起動し、現在文書の参照が残る `img` 配下の画像だけを一覧化する。同じ画像の複数参照は1ファイルにまとめる。
- 旧名→新名の一覧を表示し、文書内の初出順に「現在MD名＋連番＋元拡張子」を割り当てる。未参照ファイルを改名・削除しない。
- 拡張子は維持し、ファイル形式変換を改名に混ぜない。Windowsの禁止文字・予約名・衝突を検査する。
- 初版では、別画像が既存ファイルまたはUndo/Redo履歴で使用している名前への改名を禁止する。一括改名は空き番号へ採番し、別画像同士の名前交換は行わない。同じ画像の名前を維持する場合は変更不要とする。
- 大文字小文字だけの変更は同一画像であることを確認し、一時名を介して処理する。

**改名は、保存済みMDと画像の整合を保つ操作として実装する。** 実画像だけを先に改名し、保存済みMDが旧名を参照する状態を作らない。

保存済み文書では、改名ダイアログに本文保存を伴うことを表示する。画像の準備、本文の安全保存、画像変更の確定を一連の処理にし、失敗に備えた復旧記録と旧画像のバックアップを保持する。未保存文書では一時領域内で変更し、初回保存時に確定する。

QSaveFileは単一ファイルの安全保存に使えるが、MDと複数画像を一括で原子的に保存するものではない。途中失敗と次回起動時の復旧を別途実装する。[QSaveFile](https://doc.qt.io/qtforpython-6/PySide6/QtCore/QSaveFile.html)

通常のテキストUndoだけで参照先が消えないよう、改名操作も履歴に関連付ける。Undo/Redoに必要な旧名・新名の画像は参照が必要な期間保持し、必要に応じてバックアップから復元する。保存済みMDが指している画像も保持する。

同じフォルダの別MDからの共有参照を検出した場合は旧名を残し、現在文書用の新名コピーを作る。別MDを自動書換しない。アプリが確認できない範囲からの参照までは保証できないため、改名対象の範囲をダイアログに明示する。

## 8. 既存の表を編集

- ソースの表領域を右クリックした場合、「表を編集」を表示する。
- 表貼り付けと共通のQTableViewモデルを持つモーダルダイアログを使う。
- セル編集、行列追加・削除・移動、ヘッダー編集、列寄せ、コピー/貼り付けに対応する。
- GFM表のエスケープやインラインコードを解析してセルへ戻す。引用・リスト内の表では外側の接頭辞も保持する。
- 開始時の文書更新番号と対象範囲を記録し、確定時に更新番号を照合して表の範囲だけ置換する。不一致なら古い範囲へ適用せず、表の再取得または適用中止を行う。Undoは1回。開いただけ/キャンセル時は元ソースをそのまま保持する。
- 初版の表エディタはGFMのパイプ表を対象とする。HTML表の結合セル等を黙って平坦化しない。表形式では表現できないデータは変換結果を示す。

## 9. 文字コード・改行コードと保存

- ファイルはバイト列で読み、BOM確認、厳密なUTF-8検査、その後に文字コード推定を行う。
- UTF-8/BOM付きUTF-8、UTF-16、CP932/Shift_JIS、EUC-JP等を検証対象にする。推定が曖昧な場合は現在の選択を表示し、「文字コードを指定して開き直す」を用意する。
- 自動判定は完全ではない。無効な文字を置換して読み込み、そのまま元ファイルを上書きする処理はしない。
- CRLF/LF/CRを読込前のバイト/復号結果から判定し、内部編集はLFへ正規化する。保存時は元の文字コード・BOM・改行を基本として維持する。
- 改行混在を検出したら表示し、初回保存時に利用者が統一先を選ぶ。無断で正規化しない。
- 新規文書の既定値はUTF-8・BOMなし・LFとし、設定で変更可能にする。末尾改行の有無も保持する。
- 元の文字コードで表現できない文字を保存する場合は、UTF-8への変更等を選べるようにする。文字の欠落を伴う保存はしない。
- 外部変更を監視し、未編集なら再読み込み、編集中なら比較/再読込/別名保存へ案内して上書き競合を防ぐ。

## 10. HTML出力・出力プレビュー・PDF

### 10.1 共通の出力処理

Markdown解析 → 出力用HTML生成 → 出力CSS適用 → 画像等の埋め込み → 出力プレビュー/HTML保存/PDF生成、の順とする。

通常プレビューと出力でMarkdownの解釈を共通化し、スタイルだけを切り替える。出力プレビューには、実際に保存するHTMLと同じ成果物を表示する。

- HTMLはUTF-8で出力し、CSSを`style`に埋め込む。標準テンプレートとユーザーCSSの指定に対応する。
- ローカル画像は実際のMIMEタイプに対応したdata URIへ置換する。Markdown画像・HTMLの画像要素の両方を対象にする。
- CSSのローカル画像/フォント参照も解決・埋め込み対象とし、解決できない参照は出力前に一覧化する。
- HTMLの`srcset`/`picture`やCSSの`@import`内の参照も検査する。未対応の構文や埋め込めない依存先を残したまま、単体で再現可能と判定しない。
- 外部URL画像がある場合は、その画像の取得・埋め込みを選べるようにする。未取得・欠損のまま「画像埋め込み完了」とは扱わない。
- 出力先HTMLと元MDのフォルダが違っても、埋め込み後の表示が変わらないようにする。
- 通常プレビューのスクロール用余白、ソース行属性、QWebChannel用処理は出力に含めない。

QWebEngineViewの `setHtml()` は内部のdata URLに2MB制限があるため、画像埋め込みHTMLの出力プレビューには使わない。一時HTMLファイルを `load()` して表示する。通常プレビューも固定HTMLシェル＋本文更新等で大容量化に備える。[Qtの制限](https://doc.qt.io/qt-6/qwebengineview.html#setHtml)

本文中のHTMLは許可する要素・属性を決めて表示する。任意のscript、イベント属性などを実行せず、アプリ側JSとの境界を保つ。サニタイズは本文由来部分へ適用し、その後にアプリ管理の描画処理を加える。QWebChannelにはスクロール等の最小機能だけを公開する。[Markdown-it-pyのHTML処理方針](https://markdown-it-py.readthedocs.io/en/latest/security.html)

### 10.2 PDF

- 出力用QWebEnginePageの `printToPdf()` を利用する。
- 用紙サイズ、縦横、余白を設定可能とし、印刷用CSSで見出し・表・コードの改ページを調整する。
- 画像・フォントと、採用した場合のMermaid/数式の描画完了を待ってから生成する。
- 非同期完了通知で保存結果を確認し、生成中にページを破棄しない。[QtのPDF出力API](https://doc.qt.io/qt-6/qwebenginepage.html#printToPdf)
- HTMLの出力プレビューで改ページまで一致するとは扱わない。ページ単位のPDFプレビューは、一時PDFを生成してQtPdfWidgetsで表示する追加案とする。

## 11. PyInstaller配布と.md関連付け起動

- `md-editor.exe "C:\資料\設計メモ.md"` のような起動引数を受け取って開く。
- 受け取った相対パスは起動時のカレントディレクトリを基準に絶対化し、以後はカレントディレクトリやEXE配置先に依存させない。画像の基準は開いたMDのフォルダとする。
- 複数ファイル引数は文書ごとにウィンドウを開く。既起動時の関連付け起動は、初版では別プロセスのウィンドウとして扱い、同じMDの重複編集は外部変更検出で保護する。
- ファイル不存在、読込不可、対応外ファイルのエラーをGUIで知らせる。
- OS側で利用者が関連付けを行った後のダブルクリック起動を検証する。アプリ内の関連付け設定・レジストリ変更機能は実装しない。[Windowsのファイル関連付け](https://learn.microsoft.com/en-us/windows/win32/shell/fa-file-types)
- まずWindows上で `--onedir --windowed` を使用し、QtWebEngineProcess、Qtリソース、ローカルCSS/JS/フォント、画像形式プラグインの同梱を確認する。onefileは必要性と起動時間を見て後で検討する。[配布形式](https://pyinstaller.org/en/stable/operating-mode.html)、[同梱リソースの参照](https://pyinstaller.org/en/stable/runtime-information.html)
- 設定・ログ・復旧データはユーザーデータ領域へ置き、EXEのフォルダやPyInstaller展開先へ保存しない。
- Python未導入の環境、日本語/空白入りパス、オフラインでのプレビュー・HTML/PDF出力を配布試験に含める。

## 12. Mermaid・数式のエディタは必要か

**表示対応は追加する価値が高い。専用エディタは、ソース＋即時プレビューの小さな編集ダイアログを後から追加するのが適切。図形をドラッグする本格的な図エディタや数式のWYSIWYG入力は、初版の必須範囲にしない。**

| 機能 | 提案する扱い | 理由 |
| --- | --- | --- |
| Mermaidの表示・HTML/PDF出力 | 初版への追加候補 | 技術文書で使いやすく、既存の右プレビューを活用できる |
| 数式の表示・HTML/PDF出力 | 初版への追加候補 | 左で記法を書き、右で確認する構成と相性がよい |
| Mermaid編集ダイアログ | 次段階 | 対象ブロックだけ拡大して編集、テンプレートと構文エラー表示を付ける |
| 数式編集ダイアログ | 次段階 | 分数・行列等の挿入補助、LaTeXソースと結果の同時表示が実用的 |
| 図形の直接操作/本格数式パレット | 利用頻度を見て判断 | 記法との相互変換や独自の編集操作が大きな開発範囲になる |

Mermaidは`mermaid`コードフェンス、数式はブロック`$$...$$`とインライン記法を解析する。数式の`$`は通貨表記と競合するため、境界条件を定め、コード内を除外する。レンダラーはMermaidとKaTeXを候補とし、必要なJS/CSS/フォントをローカル同梱する。

Mermaidは制限付きの設定で描画完了を待ち、数式エラーは本文を書き換えずプレビューに表示する。[Mermaid公式](https://mermaid.js.org/config/usage.html)、[KaTeXの設定](https://katex.org/docs/options)

単体HTML出力ではMermaidを描画済みSVGとして埋め込み、KaTeXは描画済みHTML/MathMLと必要なCSS・フォントを埋め込む案を採る。外部CDNに依存せず、PDFにも同じ結果を使う。これらを採用する場合は、専用エディタより先に描画・出力・スクロール位置再計算まで一通り完成させる。

## 13. 追加で勧める機能

| 優先度 | 機能 | 目的 |
| --- | --- | --- |
| 高 | クラッシュ復旧・作業内容の自動退避 | 未保存MDと貼り付けた一時画像をセットで復元する。元MDの無条件自動上書きはしない |
| 高 | 外部更新の検出 | 他エディタや複数ウィンドウとの保存競合を防ぐ |
| 高 | 置換・全置換 | ファイル内の文章・記法をまとめて修正する |
| 中 | 見出しアウトライン | 長い文書内を移動する。必要時だけ開くサイドパネルにする |
| 中 | 画像/リンク切れの一覧 | 移動や外部編集後の参照切れを見つける |
| 中 | 最近使ったファイル・カーソル位置復元 | 日常の再編集を短くする |
| 中 | 画像ドラッグ＆ドロップ、リンク挿入 | 貼り付けと同じ画像管理処理を再利用する |
| 後 | 未使用画像の確認と整理 | 参照範囲・共有利用を確認してから手動実行する。通常保存では削除しない |
| 後 | タブ、複数ファイル検索、ワークスペース | 複数文書を扱う頻度に応じて追加する |

## 14. 実装順序と完了条件

ユーザー指定の機能はフェーズ0～6で完成させる。Mermaid/数式と追加提案は別枠とし、採用する場合は描画処理を出力フェーズの前に組み込む。

| フェーズ | 実装内容 | 次へ進む条件 |
| --- | --- | --- |
| 0 | uv初期化、依存解決、起動入口、最小PySide6ウィンドウ | uvで起動でき、依存バージョンを固定できる |
| 1 | 2ペイン、基本レンダリング、スクロール同期/末尾、最小PyInstallerビルド | 長い文書・画像・表で対応位置を追え、末尾を上端へ動かせ、配布EXEで表示できる |
| 2 | 文書の開閉・保存、文字コード/改行、インデント、ハイライト、検索、テーマ | 通常編集・IME・Undo・保存往復が成立する |
| 3 | HTML/plain判定、画像貼り付け、一時画像、CSV/TSVダイアログ、既存表編集 | 貼り付け/保存/キャンセルで本文や画像を壊さない |
| 4 | 外部画像編集、個別/一括改名、保存との整合、復旧 | 改名・Undo・共有参照・保存失敗から回復できる |
| 5 | CSS付きHTML、画像埋め込み、出力プレビュー、PDF | HTML単体で再現でき、PDFの描画完了と保存成功を確認できる |
| 6 | 配布仕上げ、回帰試験、操作説明 | Pythonなし・日本語パス・関連付け起動・オフラインの試験を通る |

重点的な受け入れ試験:

1. 見出し・折り返し段落・巨大画像・表・長いコード・末尾空行で、左右どちらから操作しても同じ内容へ追従し、同期が往復しない。
2. 最終行を左の最上部に置ける。右も対応する最終位置へ移動し、保存内容に余分な改行が入らない。
3. 参考デモの2段階Backspaceと空項目Enterを維持し、コード内のリスト風文字列、絵文字、日本語IMEでは誤処理しない。
4. チャットのコードコピーボタンがHTMLを含んでも、`_`、`*`、バックスラッシュ、インデントを変えず貼り付けられる。通常のHTML記事はMarkdownへ変換できる。
5. 引用符・カンマ・タブ・セル内改行・先頭ゼロのある表を読み、ヘッダーを選択して編集・再挿入できる。
6. 未保存文書への画像貼り付けから初回保存・別フォルダSave As・取消・保存失敗を行っても画像が消えない。
7. 同一画像の複数参照、参照形式、共有画像、既存名/履歴上の名前との衝突、大文字小文字の変更、一括改名後のUndo/Redo、途中失敗で参照を壊さない。
8. UTF-8/BOM/CP932/UTF-16とCRLF/LF/CRを往復し、変更していない内容・BOM・末尾改行を意図せず変えない。
9. 2MBを超える画像埋め込みHTMLをプレビューでき、別フォルダへ移したHTMLをオフラインで表示できる。PDFに画像抜け・描画途中・スクロール余白がない。
10. Python未導入環境で、空白・日本語入りのMDをOS関連付けから開き、画像表示・保存・PDF出力できる。

性能目標はフェーズ1で基準PCと代表文書を決めて数値化する。まず入力応答、同期時のちらつき、画像読込後の位置維持を測り、通常文書と1万行程度の長文を分けて検証する。
