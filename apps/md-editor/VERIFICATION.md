# 段階1〜6の実装・検証結果

Windows・Python 3.13・PySide6 6.11.2で検証。各確認日は以下に記載。

## プロジェクト分離後の確認（2026-09-12）

既存エディタを `apps/md-editor/` へ移動し、専用環境をlockから再作成した。移動前736件成功（67.69秒）、移動後736件成功（67.92秒）。Ruff check / formatと、専用コマンドからの起動・画像読み込み・3地点のスクロール確認も成功した。機能コードは変更していない。既知の断続的な位置復元テスト失敗は今回は発生しなかったが、継続検証の対象とする。PyInstallerの実ビルドは行っていない。

詳細は[整理作業の記録](../../docs/project-reorganization.md)を参照。今後のエディタの修正・テスト・配布はMarkNotesとは独立して進める。

## 左上のUndo／Redoボタン（2026-09-12）

- 新規・開く・保存の右に、元に戻す・やり直すを追加。既存QActionを共有し、編集履歴・メニュー・ショートカットの動作と有効状態を揃えた。
- 既存と同じ18px・線幅1.25のアウトラインアイコンを使用。無効時は薄く描画し、ライト／ダークと100%・150%・200%の描画を確認。中央のファイル名配置も維持。
- 既存の起動テストで、初期無効→編集→ボタンクリックUndo→Redo有効→Redoによる復元を検証。プレビューのみ表示中の操作も、既存のソース再表示処理を経由する。

タイトルバー・表示切替の関連17テストは初回すべて成功（11.38秒）。アイコン濃度調整後は16件成功、以前から記録のある非表示プレビューの位置復元1件が単発失敗し、単体再実行は成功。新しいUndo／Redoの操作検証は両実行で成功。Ruff check / format成功。最終画像は `artifacts/undo-redo-toolbar/light.png`、`dark.png`、`dark-redo-enabled.png`。PyInstallerのビルドは実施していない。

## 右クリックからの書式変更（2026-09-12）

- ソースの右クリックに「文字の書式」「行・ブロックの書式」を追加。文字側は太字・斜体・取り消し線・インラインコード、ブロック側は標準段落／見出し1〜6・引用・箇条書き・番号付きリスト・コードブロックを操作する。メニューは一時ポップアップが所有し、既存の共有メニューの寿命を維持。ライト／ダークの実描画を確認。
- インライン解析にソース位置を付け、記号込み／中身だけの選択、混在の統一と解除、部分解除を処理。候補は再解析して文字・他書式・リンクの意味が保たれる場合だけ採用。リンク表示文字・単一表セル・Unicode・引用の継続行に対応し、コード・数式・URLや複数段落をまたぐ選択を保護。
- インラインコード解除は範囲全体を対象とし、まず生の本文で意味を確認、必要な場合だけエスケープする。print(x)等の不要なエスケープ、entityの誤変換、表のpipeの二重エスケープを回帰確認。文字書式の処理対象は1つのinline領域20,000文字まで。
- ブロック操作は行全体へ拡張し、終端が次行先頭ならその次行を除外。外側の引用／リスト、子要素のインデント、lazy continuation、前後の段落境界を保持。Setextの標準段落化では明示改行を維持し、安全にATXへ変換できない複数行見出しは無効化。複数行inline code／数式／強調を途中で分断する変換も無効化。
- コード設定は言語自動判定と安全なフェンス長を使用し、作成後は本文だけを選択。コードや引用・リストの解除で範囲が全体へ拡張する場合はラベルへ明示。UTF-16位置変換・逆向き選択・1回Undo／Redo・古いポップアップと読み取り専用状態の編集拒否をQtで確認。
- インライン候補の遅延生成とブロックの行インデックスで右クリック処理を改善。10,000文字の段落内選択は約60ms、1,000段落の全選択でブロック操作候補の計算は約23ms。独立したMarkdown解析との159ケースの照合でも本文・他書式・リンクの整合を確認。

全736テスト成功（67.21秒）。新規はインライン65件・ブロック53件・Qt統合22件。Ruff check / format成功（78ファイル）。非表示プレビューの位置復元テストが途中の組合せ実行で一度失敗したが、単体再実行と最終全体実行は成功。確認画像・操作記録は `artifacts/formatting/light-inline-menu.png`、`dark-inline-menu.png`、`dark-code-menu.png`、`partial-removal.png`、`visual-report.json`。PyInstallerのビルドは実施していない。

## プレビューのコードハイライト・コピー（2026-09-12）

- Pygmentsでコードブロック全体を解析し、元の行アンカーを保ったまま行内のトークンを色分け。複数行文字列・コメント、Unicode、HTML記号、タブ・空行・末尾改行を検証。明示言語を優先し、未指定時は既存の言語自動判定を使用。未知言語・plain text・大きな入力は無色に戻す。
- 色だけをライト／ダークテーマで切り替え、文字の太さ・行高を維持。32,768文字・2,000行未満・16,384トークンの解析上限、トークン間100msの処理時間目安、32件のキャッシュで編集時の処理量を制限。
- コード右上に細いアウトラインのコピーボタンを追加。横スクロールの外に配置し、先頭行と重ならない余白を確保。元本文をQtのクリップボードへ渡し、コピー後は一時的にチェックマークへ切替。実マウス操作とEnterキー、空行・タブ・末尾改行の一致、古い文書のコピー拒否を確認。JavaScriptの一般クリップボード権限は無効のまま。
- HTML／PDFでは同じハイライトCSSを埋め込み、ライト配色を使用。生成専用属性でコピーボタン・囲みを除去し、同名classを持つユーザーHTMLは保持。HTMLの再表示で色と空行高を確認し、PDF生成まで通過。
- 本体アプリでライト・ダークとコピー完了表示を撮影して目視確認。テーマ切替後の内容位置と行高、横スクロール中のボタン位置、最終行の上端移動を確認。サンプルの最終位置22でscrollYと最大値507pxが一致。

全596テスト成功（66.26秒）。Ruff check / format成功（70ファイル）。確認画像と記録は `artifacts/code-preview/light.png`、`dark.png`、`copied.png`、`visual-report.json`。PyInstallerのビルドは実施していない。

## コードブロック・引用として貼り付け（2026-09-12）

- 「形式を指定して貼り付け」に「コードブロックとして貼り付け」「引用として貼り付け」を追加。アプリメニューとソースの右クリックで同じアクションを共有し、プレビューのみ表示中はソースを表示して実行する。
- コードはtext/plainを優先し、HTMLの併存で記号やインデントを変換しない。入力のバッククォートより長い外側フェンスで囲み、CRLF／CRは編集文書と同じLFへ正規化。引用は空行・既存のMarkdownを保持して各行に引用記号を追加し、HTMLのみの場合はMarkdownifyで変換する。
- 既存Pygmentsを使い、Python／JavaScript／TypeScript／JSON／HTML／XML／CSS／Bash／PowerShell／SQL／C／C++／C#／Java／Rust／Goの特徴を検査。shebang、厳密なJSONのobject／array、特徴的な構文を優先し、lexerでも確認する。共通構文だけの断片や普通の文章は言語指定なし。
- 言語解析は先頭16,384文字、lexer確認は4,096文字に制限し、コードの実行や外部送信を行わない。巨大入力を含む言語判定の66テストで代表言語・曖昧さ・解析上限を確認。
- 前後の本文から段落を分離し、選択範囲の置換と1回のUndo／Redoに対応。空・画像だけのクリップボードや読み取り専用状態で選択範囲を壊さない。
- 既存のトップレベルのコードフェンス本文を置換する場合は、選択方向によらず同じ範囲を扱い、外側の言語指定を維持。閉じフェンス前の改行を補い、入力にフェンスが含まれる場合は外側を拡張する。コード全体やインデント式コードの全選択は新しい言語付きフェンスで置き換える。

全551テスト成功（64.73秒）。Ruff check / format成功（68ファイル）。実アプリで5項目の貼り付けサブメニュー、Pythonの言語フェンス、空行を含む引用と入れ子の引用を確認。画像は `artifacts/block-paste/main.png`、`paste-menu.png`、内容と表示結果は `visual-report.json` に記録。PyInstallerのビルドは実施していない。

## スクロールバーとメニューのテーマ統一（2026-09-12）

- ソースのQtスクロールバーとプレビューのWebEngineスクロールバーを、幅12px・内側6pxの角丸つまみ・矢印なしへ統一。縦横とも同じ寸法で、背景／つまみ／ホバー／押下色をライト・ダークそれぞれで揃えた。プレビュー内のコード・数式などの横スクロールにも適用。
- 実画面で左右のバー幅が12px、横バー高が12pxで一致することを確認。両側のつまみをマウスでドラッグし、トラッククリック、双方向の内容同期、最終行の上端移動を確認。136行サンプルの最終位置135でプレビューのscrollYと最大値4931pxが一致。
- アプリメニュー・サブメニュー・右クリックメニューに、所有する文書ウィンドウのテーマを適用。既存メニューの再利用時とQt内部で生成される標準メニューの表示時に、背景・文字・選択・無効項目・区切り線を更新する。
- ソース／プレビューの右クリック、検索欄、表セル、表の行列番号のメニューを実際に表示し、描画背景色とUIフォントを確認。コード用等幅フォントがメニューへ継承されるのも解消。ダーク時のチェックマーク・サブメニュー矢印・無効項目は実画面で目視確認。
- 複数ウィンドウでライト／ダークを別々に設定し、片方の変更がもう片方やQApplicationのパレットを変えないことを検証。メニューバーの細い文字・下線なしの見た目と、既存の共有アクション・メニューの寿命は維持。

全464テスト成功（61.29秒）。メニューの実描画と複数ウィンドウ・テーマ再切替を確認する回帰テスト2件を追加。Ruff check / format成功（66ファイル）。PyInstallerのビルドは実施していない。

確認画像は `artifacts/scrollbar-menus/light.png`、`dark.png`、`menu-dark-edit.png`、`menu-dark-theme.png`、`menu-light-edit.png`。幅・色・ドラッグ・末尾位置の実測値は `scrollbar-report.json` に記録。

## 上部UIの文字・アイコン調整（2026-09-10）

- タイトルとメニューバーの文字を10pt・Light（ウェイト300）へ統一し、ツールバーアイコンは18pxへ揃えた。SVGの線幅を24単位中1.7から1.25へ変更し、18px表示時に約1pxとなる細い線で描画。アプリアイコンの表示は16pxへ調整。
- メニュー文字と表示切り替えアイコンの縦位置を揃え、Fusionが各メニュー項目の下端に描く影の線を削除。アクセスキー・選択中／押下中の表示は維持。
- テーマ変更のたびにメニューバーの色を更新し、ライト・ダークでタイトルバーと背景を揃えた。実画面で文字の太さ、線の除去、アイコンとのバランスを確認。Alt+Fによるメニュー操作も確認。

既存のタイトルバー・表示対象・貼り付け／右クリック関連42テスト成功（20.35秒）。Ruff check / format成功（64ファイル）。外観変更のため新規テストは追加せず、実画面と既存テストで検証。PyInstallerのビルドは実施していない。

確認画像は `artifacts/ui-refinement-thin/light-header.png`、`dark-header.png`（上部UI）と `light.png`、`dark.png`（全体）。フォント・寸法・アクセスキーの確認結果は `visual-report.json` に記録。

## タイトルバー・表示対象の変更（2026-09-10）

- 独立したツールバー行を廃止。タイトルバー左に既存アプリアイコンと新規・開く・保存のアウトラインアイコン、中央にファイル名と変更済み `*` を配置。アプリ名はタイトルに表示しない。
- 「表示 → 表示対象」とメニューバー右端のアイコンでソースのみ／プレビューのみ／ソース+プレビューを切り替える。メニューとボタンは同じアクションを共有し、選択を次回起動へ保存。
- 本文・選択・左右の幅・内容に基づくスクロール位置を保持。非表示中に更新されたプレビューは、再表示後の再計測・数式／Mermaid描画の完了を待って位置を復元し、古い位置通知の逆流を防止。同期オフでは左右の位置を独立して保持。
- プレビューだけ表示している時のコピー／全選択はプレビューへ適用。検索・置換・貼り付けなどはソースを表示してから実行し、処理前に別文書や別表示へ切り替わった古い編集要求は破棄。
- スクロール同期・折り返し・先頭／最終行への移動はメニューに集約。新規・開く・保存は既存アクションを共有し、右クリックの共有メニューの寿命も維持。
- 実アプリのライト／ダークと3表示を撮影して目視確認。タイトルの中心座標がタイトルバーと一致し、最大化がモニター作業領域と一致、復元後の位置とサイズが元通りになることを確認。タイトルバー単体でも200% DPIで最大化・復元とアイコン描画を確認。
- タイトルバーのダブルクリック・最小化・閉じる際の取消、移動と8方向リサイズのOS操作への接続を検証。アウトラインアイコンはSVGから描画し、小サイズ・複数DPI・有効／無効状態を確認。

全462テスト成功（62.03秒）。Ruff check / format成功（64ファイル）。表示対象・タイトルバーの新規17テストに加え、既存のスクロール、編集・保存、貼り付け・右クリック、画像管理、HTML／PDF出力の回帰試験を含む。PyInstallerのビルドは実施していない。

確認画像は `artifacts/ui-layout/light-split.png`、`dark-split.png`、`light-source.png`、`light-preview.png`。実画面の座標・最大化確認は `artifacts/ui-layout/visual-report.json` に記録。

## アプリアイコンの角丸化

- 元のPNGの絵柄を保持し、半径22%の角丸マスクを適用。元画像の中央領域が画素単位で一致することを確認。
- PNGおよびICO全9サイズを更新。ICO内の各PNGとQtの実QIconの両方で四隅のalpha=0、中央のalpha=255を確認。
- WindowsのLoadImageWでも16 / 32 / 256 pxを読み込み、全画素のalphaがICO内PNGと一致することを確認。四隅の完全透過と輪郭のアンチエイリアスを維持。
- 明暗背景で輪郭を目視確認。確認画像は `artifacts/app-icon/rounded-preview.png`、検証記録は `artifacts/app-icon/rounded-check.json`。
- アイコンを参照するアプリと配布設定は同じファイルパスを使用。PyInstallerのビルドは実施していない。

## アプリアイコン（2026-09-10）

- 内蔵画像生成でMarkdownの「M」と2ペインを組み合わせたアイコンを作成し、PNGとICOをresourcesに配置。採用画像の生成指示は `src/md_editor/resources/app-icon-design.md` に記録。
- ICOの16 / 20 / 24 / 32 / 40 / 48 / 64 / 128 / 256 pxをバイナリ構造とQtの読み戻しで確認。1254 pxのPNGもQIconへ追加し、明暗背景で小サイズを目視確認。
- QApplicationとMainWindowに同じアイコンを設定。Windows AppUserModelIDの読み戻しと、実際のHWNDの大小アイコンがともに設定されていることを確認。プロジェクト以外の作業フォルダからも画像を読み込める。
- PyInstaller specのEXEアイコンと同梱リソースを設定し、specの構文を確認。ビルドは段階7まで保留し、今回も実施していない。
- Ruff check / format成功（59ファイル）。確認画像は `artifacts/app-icon/sizes.png`、ICO確認記録は `artifacts/app-icon/icon-check.json`。

## 右クリック後に共有メニューが無効になる不具合（2026-09-08）

- 一時的な右クリックメニューへ `addMenu(existing_menu)` で挿入メニューを追加すると、右クリックメニューの遅延削除後にPySideの共有メニューラッパーが無効になることを再現。
- 既存の貼り付けサブメニューと同様に `addAction(existing_menu.menuAction())` で共有し、挿入メニューの寿命を維持。
- 従来のテストは右クリック終了直後だけを確認していた。今回、遅延削除を完了させてから複数回右クリックし、表挿入・Undo・アプリメニューからの再実行を検証する回帰テストを追加。修正前に失敗し、修正後に成功。
- 貼り付け・右クリック・画像操作・設定の関連45テスト成功（18.81秒）。Ruff check / format成功（56ファイル）。PyInstallerのビルドは実施していない。

## 設定・挿入メニュー・画像命名の整理（2026-09-08）

- スクロール検証サンプルを開くGUI項目を削除。開発用の起動オプション `--sample` は維持。
- 設定を「ファイル → 設定…」に集約。画像編集アプリ、初期フォルダ、新規文書の文字コード・改行コードを保存。設定ダイアログの確定／取消、不正入力、保存失敗時の値保持を検証。
- 起動時と次回新規作成に既定形式を適用し、CP932・BOM付きUTF-8・UTF-16とCRLF／CRの実保存を確認。既存文書の認識形式や現在の編集中文書を変更しない。ファイル選択の初期フォルダにも設定を適用。
- 一括改名でプレフィックス・開始番号・最小桁数を指定し、変更候補を一覧表示。番号の衝突回避、指定桁数を超える番号、カスタム名での本文保存・Undo／Redo・共有参照維持を検証。
- 「挿入 → 表…」から表を作成してOKで挿入。右クリックの挿入サブメニューはアプリと同じメニュー／アクションを共有。選択置換、段落分離、1回のUndo、取消、文書更新検知、ダイアログ破棄を確認。
- 実際の設定・表作成・一括改名ダイアログを描画し、レイアウトを目視確認。確認画像は `artifacts/menu-settings/settings.png`、`table-create.png`、`image-rename.png`。

全444テスト成功（68.91秒）。Ruff check / format成功（56ファイル）。PyInstallerのビルドは実施していない。

## 段階5〜6（画像操作と出力、2026-09-08）

- 現在の文書が参照するimg配下の画像だけ個別／一括改名。ラスター画像は既定ペイントまたは指定アプリで編集し、外部での置換保存・削除後の再作成をWebEngineへ反映。
- Markdown・HTML画像・picture/srcset・参照形式のURL範囲だけ更新。保存済み本文、文字コード・改行、1回のUndo／Redo、初回保存・別名保存との整合を確認。
- 同じフォルダの共有画像、名前・履歴衝突、大文字小文字のみの変更を保護。途中の書き込み失敗と外部更新が重なっても参照画像を残し、ジャーナルと画像付き復旧コピーから回復できる。破損した復旧記録や曖昧な文字コードも保守的に扱う。
- 出力画面で標準CSS＋追加CSS、外部画像取得、PDFの用紙サイズ・向き・余白を指定。HTMLプレビューとHTML保存には同一の成果物を使用。
- ローカル画像・CSSの再帰import/url/image-set/フォント・SVG依存・KaTeXフォントを埋め込み、Mermaidは描画済みSVG。通常のローカルリンクは保持し、リンク先文書は埋め込まない。未解決依存を一覧表示し、不完全な出力の保存を止める。
- 2MB超HTMLのファイルロード、保存失敗時の既存出力維持、連続更新の古い通知、出力途中の終了を検証。PDFは画像・フォント読込完了後に生成し、CSSの@pageより指定用紙・余白を優先。印刷時だけ使うフォント・背景画像も実PDFへ含まれることを確認。
- 日本語・画像・インライン／ブロック数式・行列・Mermaid・表・コード・CSS改ページを含むA4/2ページのPDFを生成し、Popplerで全ページを画像化して目視確認。別フォルダへ移したHTMLもオフラインで表示できる。

全414テスト成功（62.26秒）。Ruff check / format成功（54ファイル）。PyInstallerのビルドは実施していない。

成果物と診断記録:

- `artifacts/stage56/output.html` / `output.pdf`: 実画面から保存した検証文書。
- `artifacts/stage56/output-preview.png` / `pdf-page-1.png` / `pdf-page-2.png`: 出力プレビューと全PDFページの確認画像。
- `artifacts/stage56/image-rename-review.png`: 共有参照を含む一括改名のレビュー画面。実MD／画像ファイルの更新、Undo／Redo、共有旧画像の保持を確認。
- `artifacts/stage56/scroll-smoke.json`: ソース版136行サンプル。位置0／63／135で同期、最終scrollYと上限4885pxが一致、ok=true。

## インライン数式の表示修正（2026-09-08）

- `$ x_i $` のように区切り記号の内側に空白があるインライン数式を認識。片側の空白・タブ・NBSPも検証。
- 通常貼り付けで共有数式パーサーを使ってTeXを判定し、HTMLが併存してもtext/plainを保持。Markdownifyによる下付き文字の不要なエスケープを防止。
- 修正前に6件の失敗を再現し、閉じる `$` の入力直後の描画、本文・強調・表内の式、実クリップボードからの貼り付けとUndo／RedoをWebEngineで検証。生成された数式の可視領域と元のTeXを確認。
- 既存の通貨表記・コード・エスケープの判定、数式・Mermaid描画後のスクロール同期を含む全326テスト成功（46.14秒）。Ruff check / format成功（42ファイル）。

PyInstallerのビルドは実施していない。

## TeX・Mermaidと貼り付けメニュー（2026-09-08）

- KaTeX 0.18.7で `$…$`、`$$…$$`、`\(...\)`、`\[...\]` を描画。Mermaid 11.17.2で `mermaid` フェンスをSVG描画。
- JavaScript/CSS/フォントをローカル同梱し、127ファイルのSHA-256とフォント参照を検証。取得元・固定版・ライセンスはresources/vendorに保存。
- 分数・総和・行列、日本語フローチャート・シーケンス図の実画面をライト／ダークで確認。
- 非同期描画とフォント読み込みの完了後にソース位置対応を再計算。古い更新の反映を防ぎ、テーマ変更・ペイン幅変更・左右同期・末尾移動を検証。
- 数式・図の構文エラーは本文を保持したままプレビュー内に表示し、修正後に復帰。
- TeX内のMarkdown風の画像・リンク・表を、保存や表編集の対象にしないよう解析を統一。
- 編集メニュー・右クリックの「貼り付け」直下に「形式を指定して貼り付け」を設け、plain／Markdown／表の3項目を集約。アプリの「表を編集」は削除し、実際の表上の右クリックに限定。

全309テスト成功（44.29秒）、Ruff check / format成功（42ファイル）。`uv run --no-sync md-editor examples/math-mermaid.md --smoke-report artifacts/math-mermaid-smoke.json` で45行の文書を開き、ソース位置0／26／44と最終スクロール上限1200pxの同期を確認。画面は `artifacts/math-mermaid-light.png` と `artifacts/math-mermaid-dark.png` に保存。

この時点ではHTML/PDFへの統合は未実装だったが、段階6で対応済み。数式・Mermaidの専用エディタは追加しない。PyInstallerのビルドは実施していない。

## UI調整（2026-09-08）

- 左右ペインの見出し帯を削除し、編集・表示領域を拡大。
- ソースの右クリックを日本語化し、アプリの編集メニューと同じアクション・順序・有効状態を使用。
- Qtの日本語翻訳を読み込み、検索欄・表のセル編集欄の標準メニューも日本語化。プレビューには日本語の文書操作メニューを実装。
- 表の最終列の自動伸長を解除。ウィンドウを広げても手動指定した列幅を保持。
- 表の下部操作バーを削除。行番号の右クリックから上／下への挿入、列番号から左／右への挿入を選択できる。削除・移動・列寄せも番号の右クリックへ移動。

調整後の全248テストが成功（36.25秒）、Ruff check / formatも成功（33ファイル）。実マウス操作で番号メニューからの列挿入と列幅変更を確認。画像は `artifacts/ui-refinement-main.png`、`artifacts/ui-refinement-source-menu.png`、`artifacts/ui-refinement-table.png` に保存。今回もPyInstallerのビルドは行わない。

## 実装済み

- 新規・開く・保存・別名保存、未保存確認、単一MDの安全な置換保存。
- BOM・文字コード・改行の自動認識、形式変更、指定した文字コードでの再読込、外部更新の検出。
- Markdownの構文ハイライト、参考デモに準じたインデント操作、IME・絵文字・Undo対応。
- 検索・置換・全置換、正規表現、ライト／ダーク／システム追従。
- HTML/plainの貼り付け判定、HTMLのMarkdown変換、明示的な貼り付けモード。
- クリップボード画像・画像ファイル、未保存時の一時管理、初回保存・別名保存での参照解決。
- CSV／TSVの表貼り付け、ヘッダー選択、既存GFM表のモーダル編集。
- 保存・別名保存とUndo／Redoをまたぐ画像参照の管理。同じURL文字列でも履歴から復元した参照と新しく入力した参照を区別。

## 今回の検証

| 検証 | 結果 |
| --- | --- |
| pytest全体 | 414件成功（段階5〜6、62.26秒） |
| Ruff check / format | 成功（54ファイル） |
| ソース版WebEngine診断 | 成功。136行、画像読み込み、先頭・中央・末尾の同期 |
| 文字コード・改行・BOM・末尾改行の保存往復 | 自動試験成功 |
| 保存キャンセル・文字コード変換失敗・外部競合 | 本文と既存ファイルを維持する試験成功 |
| 初回画像保存・別名保存・参照衝突・Undo／Redo | 自動試験成功 |
| HTML/plain/コード判定・画像の実クリップボード貼り付け | 自動試験成功 |
| 表の確定・キャンセル・Undo・引用／リスト内の編集 | 自動試験成功 |
| 検索の絵文字・正規表現・ゼロ幅・置換Undo | 自動試験成功 |
| ライト／ダークのメイン画面・表ダイアログ | 実ウィンドウを撮影して確認 |
| PyInstaller再ビルド | ユーザー指定により段階7まで実施しない |

別ドライブへの移行は相対パス計算が失敗する条件を再現して試験しています。実際に別のドライブ上で手操作した試験は未実施です。

今回の診断は `artifacts/stage34-smoke.json`、画面は `artifacts/stage34-light.png`、`artifacts/stage34-dark.png`、`artifacts/stage34-table-light.png`、`artifacts/stage34-table-dark.png` に保存。

スクロールの旧44テストも回帰試験に含めています。IMEはQInputMethodEventで変換中のキー処理を検証しています。実際の日本語IMEでの連文節変換等は利用環境で確認してください。従来の文字コード推定とHTML/plain判定はヒューリスティックで、UIから明示指定できます。

画像の外部編集・改名・改名中断の復旧、HTML/PDF出力は段階5〜6で実装済みです。通常編集全般の自動退避・クラッシュ復旧は追加提案として残っています。未保存文書を明示的に破棄すると一時画像を片付けます。PyInstaller成果物は旧段階のものなので、新しい機能はソース版で確認します。

---

# 段階1〜2の過去の検証記録

2026-09-07、Windows上で検証。

## 実装済み

- uvプロジェクト初期化と依存固定。
- PySide6の左右2ペイン、QPlainTextEditソース、QWebEngineViewプレビュー。
- ソース行と表示要素の対応に基づく双方向スクロール同期。
- 最終行を上端まで移動する操作と、表示専用の末尾余白。
- 折り返し、長い最終論理行、画像の高さ、表行、コード行への対応。
- 行番号、同期のオン/オフ、折り返し切替、ペイン幅変更、検証サンプル。
- UTF-8のMDをダイアログ/起動引数で開く処理とライブ編集プレビュー。
- PyInstaller onedir/windowedビルド。

保存、文字コードの自動判定、シンタックスハイライト、インデント等は後続段階。元のMDファイルを変更する処理は未実装。

## 実行結果

| 検証 | 結果 |
| --- | --- |
| pytest全体 | 44件成功（最終実行18.39秒） |
| Ruff check / format | 成功 |
| ソース版の実画面診断 | 成功 |
| PyInstallerのビルド | 成功 |
| EXE内のHTML/CSS/JS・画像の読み込み | 成功 |
| EXEからの先頭・中央・末尾の同期 | 成功 |
| 日本語・空白入りMDパスを別CWDから開く | 成功 |
| Python未導入の別PC/VM | 未実施 |
| Windowsの既定アプリ設定の変更 | 実施しない。ファイルパス引数による起動を検証 |

凍結EXEの検証では136行の文書と800×1200の画像を使用。0基準のソース位置0/63/135を確認し、最後の位置ではソース・プレビューとも135、右のスクロール位置と最大値は4884pxで一致。

詳細な実測値は `artifacts/source-smoke.json`、`artifacts/frozen-smoke.json`、`artifacts/frozen-japanese-path.json` に保存。先頭・画像・末尾の画面も `artifacts` に保存した。

Qtの折り返し位置は表示行単位に丸める。段落・画像の内部や空行等の対応はアンカー間の補間であり、左右のすべての文字が同じ高さに並ぶ仕様ではない。

## 手動確認

```powershell
uv run md-editor --sample
```

または `dist/md-editor/md-editor.exe` を起動する。配布時は `dist/md-editor` フォルダ全体を使用する。

1. 左右それぞれのホイールとスクロールバーで移動する。
2. 表・コード・縦長画像の前後で内容の対応を確認する。
3. 「最終行を上端へ」を実行する。
4. 中央の仕切りを動かし、折り返しを切り替えて確認する。
5. 「スクロール同期」をオフにすると左右を独立して比較できる。
